---
import { Picture } from 'astro:assets'
import TechClip from './TechClip.astro'
import TechClipSection from './TechClipSection.astro'

interface Props {
  field: Field
}
---

<div class='flex flex-col justify-center items-center text-center gap-3'>
  <!-- <Picture
    src={Astro.props.field.icon}
    alt={Astro.props.field.name}
    widths={[200, 400, 600]}
    sizes='(max-width: 800px) 100vw, 620px'
    loading='eager'
    format='avif'
    class='max-h-[250px] w-auto'
  /> -->
  <h3 class='text-4xl font-bold uppercase mb-5'>{Astro.props.field.name}</h3>
  <TechClipSection style='justify-center' technologies={Astro.props.field.technologies} />
</div>
